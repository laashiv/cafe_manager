<section style="padding-top: 100px">

    <% orders.each do |order| %>

        <% if !(order.delivered_at) %>

        <div class=<%= order.delivered_at ? "orders-List": "orders-list" %> >

        <% if !(order.delivered_at)  %>

        <div class="deliver-order">
    
            <%= form_with(url: "/orders/#{order.id}", method: "PUT") do %>

            <%= submit_tag("deliver?" , class: "deliver") %>
    
            <% end %>

        </div>

        <% end %>
        <p class="order-id">#chefoodu$00<%= order.id %></p>
        <p class="order-date"><%= order.date.to_s(:short) %></p>
        <p class="order-name">name</p>

            <% order_items.each do |order_item| %>

                <% if order.id == order_item.order_id %>

                    <div class="order-items">

                    <p style="margin-left: 50px, width: 150px"><%= order_item.menu_item_name %></p>

                    <div class="order-items-quantity" >

                        <p><%= order_item.menu_item_price %></p>
                        <p>x</p>
                        <p><%= order_item.quantity %></p>

                    </div>
                    
                    </div>

                <% end %>

            <% end %>

            <div style="display: flex">

            <% if (order.delivered_at)  %>

            <p style="padding-left: 350px">delivered at:</p>

            <p style="padding-left: 10px"><%= order.delivered_at.to_s(:short) %></p>

            <p class="order-total"> BILL : <%= order.total %></p>

            <% else %>

            <p style="padding-left: 700px"> BILL : <%= order.total %></p>

            <% end %>

            </div>

        </div>

        <% end %>

   <% end %>




   <% orders.each do |order| %>

        <% if order.delivered_at%>

    <div class=<%= order.delivered_at ? "orders-List": "orders-list" %> >

    <% if !(order.delivered_at)  %>

    <div class="deliver-order">

        <%= form_with(url: "/orders/#{order.id}", method: "PUT") do %>

        <%= submit_tag("deliver?" , class: "deliver") %>

        <% end %>

    </div>

    <% end %>
    <p class="order-id">#chefoodu$00<%= order.id %></p>
    <p class="order-date"><%= order.date.to_s(:short) %></p>
    <p class="order-name">name</p>

        <% order_items.each do |order_item| %>

            <% if order.id == order_item.order_id %>

                <div class="order-items">

                <p style="margin-left: 50px, width: 150px"><%= order_item.menu_item_name %></p>

                <div class="order-items-quantity" >

                    <p><%= order_item.menu_item_price %></p>
                    <p>x</p>
                    <p><%= order_item.quantity %></p>

                </div>
                
                </div>

            <% end %>

        <% end %>

        <div style="display: flex">

        <% if (order.delivered_at)  %>

        <p style="padding-left: 350px">delivered at:</p>

        <p style="padding-left: 10px"><%= order.delivered_at.to_s(:short) %></p>

        <p class="order-total"> BILL : <%= order.total %></p>

        <% else %>

        <p style="padding-left: 700px"> BILL : <%= order.total %></p>

        <% end %>

        </div>

    </div>

    <% end %>

<% end %>

    



</section>